---
phase: 05-documentation-and-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - spec/mcpack-spec-v1.md
autonomous: true
requirements:
  - PKG-03
  - PKG-04
  - PKG-05
  - PKG-06

must_haves:
  truths:
    - "README documents wrap mode usage with a complete TypeScript code example"
    - "README documents build mode usage with a complete TypeScript code example"
    - "README includes token reduction numbers matching test/harness/report.json exactly"
    - "Spec document exists at /spec/mcpack-spec-v1.md and is referenced in README"
  artifacts:
    - path: "README.md"
      provides: "Complete package documentation with examples and metrics"
      contains: "mcpack("
    - path: "spec/mcpack-spec-v1.md"
      provides: "Protocol specification"
      contains: "MCPack Specification v1.0"
  key_links:
    - from: "README.md"
      to: "spec/mcpack-spec-v1.md"
      via: "markdown link"
      pattern: "spec/mcpack-spec-v1.md"
    - from: "README.md"
      to: "test/harness/report.json"
      via: "hardcoded numbers extracted at write time"
      pattern: "80.7%"
---

<objective>
Write README.md with usage examples and real token reduction numbers, and commit the spec document to /spec/.

Purpose: Fulfill all four PKG requirements (03-06) so the package is ready for npm publishing with complete documentation.
Output: README.md, spec/mcpack-spec-v1.md
</objective>

<execution_context>
@/home/zaid/projects/MCPack/.claude/get-shit-done/workflows/execute-plan.md
@/home/zaid/projects/MCPack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-and-release-prep/05-CONTEXT.md

<interfaces>
<!-- Key exports from src/index.ts that README examples must reference -->

From src/index.ts:
```typescript
export { mcpack } from './wrap.js';
export { createMCPackServer } from './build.js';
export type { MCPackConfig, MCPackServerConfig, MCPackToolDefinition, MCPackHandlerContext, MCPackServer, RoleConfig, IndexConfig, SessionConfig, SearchToolResponse, SearchResult, ToolCallResult, MCPackHandle } from './types.js';
```

From src/wrap.ts:
```typescript
export async function mcpack(server: Server, config: MCPackConfig = {}): Promise<MCPackHandle>
```

From src/build.ts:
```typescript
export function createMCPackServer(config: MCPackServerConfig): MCPackServer
```

From src/types.ts:
```typescript
export interface MCPackConfig { roles?: RoleConfig; defaultRole?: string; index?: IndexConfig; session?: SessionConfig; }
export interface MCPackServerConfig extends MCPackConfig { name: string; version: string; tools: MCPackToolDefinition[]; }
export interface MCPackToolDefinition extends Tool { handler: (args: Record<string, unknown>, ctx: MCPackHandlerContext) => Promise<unknown>; }
export interface MCPackHandlerContext { toolName: string; sessionId: string; role: string | undefined; }
export interface MCPackHandle { destroy(): void; stats(): { sessions: number; tools: number }; }
export interface MCPackServer { server: Server; handle: MCPackHandle; }
```

From package.json:
```json
{ "name": "mcpack", "version": "0.1.0", "peerDependencies": { "@modelcontextprotocol/sdk": "^1.0.0" } }
```

From test/harness/report.json (token reduction data):
```json
{
  "stripe_tool_count": 28,
  "queries": [
    { "query": "create a payment", "vanilla_tool_count": 28, "mcpack_tool_count": 5, "vanilla_chars": 33258, "mcpack_chars": 4158, "char_reduction_pct": 87.5, "estimated_tokens_vanilla": 8315, "estimated_tokens_mcpack": 1040, "estimated_tokens_saved": 7275 },
    { "query": "manage customers", "vanilla_tool_count": 28, "mcpack_tool_count": 3, "vanilla_chars": 33258, "mcpack_chars": 7933, "char_reduction_pct": 76.1, "estimated_tokens_vanilla": 8315, "estimated_tokens_mcpack": 1984, "estimated_tokens_saved": 6331 },
    { "query": "subscription billing", "vanilla_tool_count": 28, "mcpack_tool_count": 5, "vanilla_chars": 33258, "mcpack_chars": 13115, "char_reduction_pct": 60.6, "estimated_tokens_vanilla": 8315, "estimated_tokens_mcpack": 3279, "estimated_tokens_saved": 5036 },
    { "query": "issue refund", "vanilla_tool_count": 28, "mcpack_tool_count": 3, "vanilla_chars": 33258, "mcpack_chars": 3196, "char_reduction_pct": 90.4, "estimated_tokens_vanilla": 8315, "estimated_tokens_mcpack": 799, "estimated_tokens_saved": 7516 },
    { "query": "list invoices", "vanilla_tool_count": 28, "mcpack_tool_count": 5, "vanilla_chars": 33258, "mcpack_chars": 3650, "char_reduction_pct": 89.0, "estimated_tokens_vanilla": 8315, "estimated_tokens_mcpack": 913, "estimated_tokens_saved": 7402 }
  ],
  "aggregate": {
    "total_vanilla_chars": 166290, "total_mcpack_chars": 32052, "overall_reduction_pct": 80.7,
    "total_estimated_tokens_vanilla": 41575, "total_estimated_tokens_mcpack": 8015, "total_estimated_tokens_saved": 33560
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy spec document to /spec/</name>
  <files>spec/mcpack-spec-v1.md</files>
  <read_first>
    - mcpack-spec-v1.md
  </read_first>
  <action>
    Create directory `spec/` and copy `mcpack-spec-v1.md` from repo root to `spec/mcpack-spec-v1.md`. The file content is identical -- this is a straight copy, not a modification. Per D-19.
  </action>
  <verify>
    <automated>diff mcpack-spec-v1.md spec/mcpack-spec-v1.md</automated>
  </verify>
  <acceptance_criteria>
    - `spec/mcpack-spec-v1.md` exists
    - `diff mcpack-spec-v1.md spec/mcpack-spec-v1.md` produces no output (files identical)
  </acceptance_criteria>
  <done>Spec document exists at spec/mcpack-spec-v1.md, identical to source</done>
</task>

<task type="auto">
  <name>Task 2: Write README.md with examples and token reduction numbers</name>
  <files>README.md</files>
  <read_first>
    - test/harness/report.json
    - src/index.ts
    - src/types.ts
    - src/wrap.ts
    - src/build.ts
    - package.json
    - .planning/phases/05-documentation-and-release-prep/05-CONTEXT.md
  </read_first>
  <action>
    Write README.md with the following exact structure (per D-01 lean structure, D-03 no badges/logo):

    **Section 1: Title and motivation (per D-02)**
    - `# MCPack` heading
    - One-line description: "Lazy, queryable, session-aware tool discovery for MCP servers."
    - One paragraph explaining the token waste problem: MCP clients receive full tool schemas at connection time via tools/list. For servers with 20+ tools, this dumps thousands of tokens into context before a single tool is called. MCPack replaces this with on-demand, query-based discovery.

    **Section 2: Install**
    ```
    npm install mcpack
    ```
    Note peer dependency: `@modelcontextprotocol/sdk ^1.0.0`

    **Section 3: Wrap Mode (PKG-03, per D-04, D-05, D-06, D-07)**
    Three-block story:

    Block 1 -- Setup code (TypeScript only, MCPack-specific lines with `// your existing server` comments):
    ```typescript
    import { mcpack } from 'mcpack';

    // your existing MCP server
    const server = createMyServer();

    const handle = await mcpack(server, {
      roles: {
        default: ['create_payment', 'list_customers', 'get_invoice', 'create_refund', 'list_subscriptions'],
        admin: ['*'],
      },
      defaultRole: 'default',
    });

    // connect transport as usual
    server.connect(transport);
    ```

    Block 2 -- Example search_tools call:
    ```json
    {
      "name": "search_tools",
      "arguments": { "query": "create a payment", "limit": 3 }
    }
    ```

    Block 3 -- Example response JSON showing the SearchToolResponse shape with tools array, total_available, showing, session_id. Use realistic tool names like create_payment, get_invoice. Show one tool with `loaded: false` and full schema, another with `loaded: true` (no schema) to demonstrate session awareness.

    **Section 4: Build Mode (PKG-04, per D-04, D-05, D-06, D-07)**
    Three-block story:

    Block 1 -- Setup code:
    ```typescript
    import { createMCPackServer } from 'mcpack';

    const { server, handle } = createMCPackServer({
      name: 'payments-server',
      version: '1.0.0',
      tools: [
        {
          name: 'create_payment',
          description: 'Create a new payment intent',
          inputSchema: {
            type: 'object',
            properties: {
              amount: { type: 'number', description: 'Amount in cents' },
              currency: { type: 'string', description: 'ISO currency code' },
            },
            required: ['amount', 'currency'],
          },
          handler: async (args, ctx) => {
            return { id: 'pi_123', amount: args.amount, currency: args.currency };
          },
        },
        // ... more tools
      ],
    });

    server.connect(transport);
    ```

    Block 2 -- Same search_tools call example (or brief note that it works identically to wrap mode).

    Block 3 -- Example response (brief, or note "same response format as wrap mode").

    **Section 5: Before / After (per D-08, D-12)**
    Before/after comparison block as hero visual:

    **Before (vanilla MCP):** Agent connects -> receives all 28 tool schemas -> 8,315 tokens in context.

    **After (MCPack):** Agent connects -> receives 1 tool (search_tools) -> searches "create a payment" -> receives 5 relevant schemas -> 1,040 tokens.

    Use the actual numbers from report.json for the "create a payment" query.

    **Section 6: Token Reduction (PKG-05, per D-09, D-10, D-11, D-12, D-13)**

    Headline stat (per D-09): "Reduces tool discovery tokens by ~80% (60-90% depending on query breadth)."

    Framing (per D-11): "Measured on Stripe MCP (28 tools)"

    Real harness console output (per D-20, D-21, D-22, D-23). Label: "Real measurement against Stripe MCP (28 tools)." Format as code block showing the actual output the harness produces. Reconstruct from report.json data:

    ```
    === MCPack Token Reduction Report ===

    Stripe MCP tools discovered: 28

    Query: "create a payment"
      Tools: 28 vanilla -> 5 MCPack
      Chars: 33258 -> 4158 (87.5% reduction)
      Est. tokens: 8315 -> 1040 (saved ~7275)

    Query: "manage customers"
      Tools: 28 vanilla -> 3 MCPack
      Chars: 33258 -> 7933 (76.1% reduction)
      Est. tokens: 8315 -> 1984 (saved ~6331)

    Query: "subscription billing"
      Tools: 28 vanilla -> 5 MCPack
      Chars: 33258 -> 13115 (60.6% reduction)
      Est. tokens: 8315 -> 3279 (saved ~5036)

    Query: "issue refund"
      Tools: 28 vanilla -> 3 MCPack
      Chars: 33258 -> 3196 (90.4% reduction)
      Est. tokens: 8315 -> 799 (saved ~7516)

    Query: "list invoices"
      Tools: 28 vanilla -> 5 MCPack
      Chars: 33258 -> 3650 (89% reduction)
      Est. tokens: 8315 -> 913 (saved ~7402)

    --- Aggregate ---
    Total chars: 166290 -> 32052
    Overall reduction: 80.7%
    Total est. tokens saved: 33560
    ```

    Then the per-query breakdown table (per D-10):

    | Query | Vanilla Tokens | MCPack Tokens | Reduction |
    |-------|---------------|---------------|-----------|
    | create a payment | 8,315 | 1,040 | 87.5% |
    | manage customers | 8,315 | 1,984 | 76.1% |
    | subscription billing | 8,315 | 3,279 | 60.6% |
    | issue refund | 8,315 | 799 | 90.4% |
    | list invoices | 8,315 | 913 | 89.0% |
    | **Aggregate** | **41,575** | **8,015** | **80.7%** |

    Add caveat (per D-11): "Results vary by server size and query breadth -- larger tool surfaces see greater reduction."

    Note from report.json: "Numbers represent character counts of serialized JSON payloads, not actual LLM tokens. Estimated tokens use chars/4 approximation."

    **Section 7: Roadmap**
    Brief bullet list:
    - v1.0: Keyword search, session tracking, role filtering (this release)
    - v1.1: Semantic search, tool usage analytics
    - v2.0: Binary encoding layer

    **Section 8: Spec link (PKG-06)**
    Link to spec: "See the [full specification](spec/mcpack-spec-v1.md) for protocol details, architecture, and configuration reference."

    **Section 9: License**
    MIT (standard for npm packages)

    IMPORTANT: All token numbers MUST match test/harness/report.json exactly. Do not round, do not approximate, do not change any number. The values in the interfaces block above are the source of truth.
  </action>
  <verify>
    <automated>grep -c "mcpack(" README.md && grep -c "createMCPackServer" README.md && grep -c "80.7%" README.md && grep -c "spec/mcpack-spec-v1.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `mcpack(` (wrap mode example)
    - README.md contains `createMCPackServer(` (build mode example)
    - README.md contains `80.7%` (aggregate reduction from report.json)
    - README.md contains `87.5%` (create a payment reduction)
    - README.md contains `90.4%` (issue refund reduction)
    - README.md contains `8,315` or `8315` (vanilla token count)
    - README.md contains `spec/mcpack-spec-v1.md` (spec link)
    - README.md contains `npm install mcpack`
    - README.md contains `search_tools` (the meta-tool name)
    - README.md contains `Stripe MCP` (measurement framing)
    - README.md does NOT contain `badge` or `Contributing` or `logo` (per D-03)
    - README.md contains `import { mcpack }` (TypeScript example, per D-05)
    - README.md contains `create_payment` (realistic tool name, per D-06)
  </acceptance_criteria>
  <done>README.md has wrap mode example, build mode example, real token reduction numbers from report.json, and spec link</done>
</task>

</tasks>

<verification>
- `cat spec/mcpack-spec-v1.md | head -1` shows "# MCPack Specification v1.0"
- `grep "mcpack(" README.md` finds wrap mode example
- `grep "createMCPackServer" README.md` finds build mode example
- `grep "80.7" README.md` confirms aggregate reduction number
- `grep "spec/mcpack-spec-v1.md" README.md` confirms spec reference
</verification>

<success_criteria>
All four PKG requirements satisfied:
- PKG-03: README documents wrap mode with code example
- PKG-04: README documents build mode with code example
- PKG-05: README includes token reduction numbers from harness
- PKG-06: Spec at /spec/mcpack-spec-v1.md, referenced in README
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-release-prep/05-01-SUMMARY.md`
</output>
